import { IOption } from '../components/MarketplacesList/option';
import { MarketplacesListOptionType } from '../types';

export const MARKETPLACE_DEFAULT_OPTION: IOption = {
  label: 'Preview Campaign & Custom Marketplaces',
  type: MarketplacesListOptionType.Empty,
};

export const DEFAULT_GIFT_CARD_PRICE = 50;
export const DEFAULT_DONATION_PRICE = 50;
